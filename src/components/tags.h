#pragma once

#include "raylib.h"

namespace tag {
    struct Player {};
    struct Weapon {};
    struct Projectile {};
    // struct Bullet {};
    // struct Grenade {};
    struct Enemy {};
    struct Destroy {};
    struct Terrain {};

    struct DropsLoot {};
}